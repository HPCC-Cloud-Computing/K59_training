//The API URLs

//the base URL to get the platform, things, items, history
var sensor_API = "http://0.0.0.0:5000";

//the base URL to post and get the rules
var rule_API = "http://f4937719.ngrok.io";

//the operator list that can be chose to add a new condition
var operator_button_list = ["AND", "OR", "operator"];

var action_list = ["update", "send_alarm", "send_alert"];

var items_can_action = ["Green Light", "Yellow Light", "Red Light"];
